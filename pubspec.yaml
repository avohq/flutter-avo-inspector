name: avo_inspector
description: Avo Inspector for Flutter.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  shared_preferences: ^2.0.12
  uuid: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

